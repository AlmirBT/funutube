/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/funutube",
  assetPrefix: "/funutube/",
  trailingSlash: true,
};

module.exports = nextConfig;
